# 15 — Testing

Minimum before completing any feature:
```bash
npx prisma validate
npx prisma generate
npm run build
```

Critical modules need strong tests: payroll, accounting, workflow, documents, permissions, authentication.
